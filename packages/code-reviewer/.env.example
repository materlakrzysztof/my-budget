# CI's ai-code-review.yml workflow consumes this same key via the
# OPENROUTER_API_KEY repository secret (see .github/actions/ai-code-review).
OPENROUTER_API_KEY=###
OPENROUTER_MODEL=anthropic/claude-sonnet-5
